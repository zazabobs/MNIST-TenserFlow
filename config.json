
---

## ⚙️ config.json (karışık örnek)

```json
{
  "model": {
    "type": "DenseNN",
    "layers": [
      {"units": 128, "activation": "relu"},
      {"dropout": 0.2},
      {"units": 10, "activation": "softmax"}
    ]
  },
  "training": {
    "epochs": 5,
    "batch_size": 64,
    "optimizer": "adam",
    "loss": "sparse_categorical_crossentropy",
    "metrics": ["accuracy"]
  },
  "data": {
    "dataset": "MNIST",
    "normalize": true,
    "shuffle": true,
    "validation_split": 0.1
  },
  "logging": {
    "save_model": true,
    "model_path": "models/mnist_model.h5",
    "tensorboard": true,
    "log_dir": "logs/fit/"
  },
  "extra": {
    "random_seed": 42,
    "use_gpu": true,
    "notes": "Karışık config, bazı parametreler kullanılmayabilir."
  }
}
